# ADB 模式支持 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增以 ADB 操作設備(模擬器/真機)的模式,與現有 Windows 原生模式並存,經 `config.json` 的 `platform` 切換,且模板/ROI/matcher/constants 全不變。

**Architecture:** 在 capture/input 之上引入統一的 `DeviceBackend` 門面(截圖+輸入+縮放+生命週期),把 `hwnd` 從 flow/state/worker 中徹底移除;`create_device(config)` 工廠依 `platform` 建立Windows 或 ADB 後端。capture 恆輸出 (1080,1920),點擊吃參考座標由後端內部縮放 → 上層平台無關。

**Tech Stack:** Python 3.9+、PyQt6、OpenCV、pywin32、adb(子進程)、pytest

**對應設計文檔:** `docs/superpowers/specs/2026-06-20-adb-mode-design.md`

---

## 檔案結構

| 檔案 | 動作 | 職責 |
|------|------|------|
| `device/__init__.py` | 新建 | `create_device(config)` 工廠 + 重導出 `DeviceError`/`DeviceBackend` |
| `device/base.py` | 新建 | `DeviceError`、純函數(`parse_wm_size`/`parse_devices`/`scale_ref_to_device`/`select_serial`)、`DeviceBackend` ABC |
| `device/adb_client.py` | 新建 | `AdbClient`:adb 子進程封裝(路徑定位、connect、devices、exec-out、wm size、tap/swipe) |
| `device/adb.py` | 新建 | `AdbDeviceBackend` |
| `device/windows.py` | 新建 | `WindowsDeviceBackend` + `find_game_window`(自 worker 遷入) |
| `automation/state.py` | 修改 | `hwnd`→`device`,移除 `capture_method` |
| `automation/flow.py` | 修改 | `input_backend`→`device`;截圖/點擊/前置改走 device;移除 win32gui 依賴 |
| `worker.py` | 修改 | 改用 `create_device` 組裝;`device.close()` 收尾;移除 `find_game_window`/`close_all` 直接呼叫 |
| `config.py` | 修改 | 新增 `platform`/`adb_*` 欄位 + 校驗 |
| `main.py` | 修改 | 依 `platform` 決定是否提權 |
| `tests/fakes.py` | 新建 | `FakeDevice` 測試替身 |
| `tests/test_config.py` | 修改 | platform/adb_* 欄位測試 |
| `tests/test_device.py` | 新建 | device 層純邏輯 + factory + 後端縮放/解析測試 |
| `tests/test_state.py` | 修改 | `hwnd`→`device`;移除 capture_method 測試 |
| `tests/test_flow.py` | 修改 | mock 改注入 `FakeDevice` |

**介面契約(跨任務固定,勿改名):**
- `DeviceBackend`: `capture()->ndarray(1080,1920,3)` / `click(ref_x,ref_y)` / `double_click(ref_x,ref_y)` / `swipe(x1,y1,x2,y2,duration=0.1)` / `prepare()` / `close()` / `resolution` property
- `parse_wm_size(text)->(w,h)` / `parse_devices(text)->list[str]` / `scale_ref_to_device(ref_x,ref_y,dev_w,dev_h)->(x,y)` / `select_serial(available,preferred)->str`
- `AdbClient(adb_path=None, connect=None, serial=None)`
- `AdbDeviceBackend(client)` / `WindowsDeviceBackend(hwnd, capture_method="auto")` / `create_device(config)->DeviceBackend`
- `ShopContext(device, mode, expect_num, money, stone, ...)`(無 hwnd/capture_method)
- `ShopFlow(ctx, templates, matcher, device, logger, config)`(input_backend→device)

**執行前:** 確認 `pytest` 全綠作為基線。REF 常數單一真值來源在 `capture`(`REF_WIDTH=1920`/`REF_HEIGHT=1080`),`device/base.py` 以 `from capture import REF_WIDTH, REF_WIDTH` 取用(本工具恆於 Windows+pywin32 環境運行,import 安全)。

---

## Task 1: config 新欄位 + 校驗

**Files:**
- Modify: `config.py`
- Test: `tests/test_config.py`

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_config.py` 末尾新增:

```python
def test_default_platform_is_windows():
    cfg = AppConfig._from_dict({})
    assert cfg.platform == "windows"


def test_platform_from_dict():
    cfg = AppConfig._from_dict({"platform": "adb"})
    assert cfg.platform == "adb"


def test_adb_fields_default_none():
    cfg = AppConfig._from_dict({})
    assert cfg.adb_path is None
    assert cfg.adb_serial is None
    assert cfg.adb_connect is None
    assert cfg.adb_screenshot_method == "screencap"


def test_adb_fields_from_dict():
    raw = {
        "adb_path": "C:/tools/adb.exe",
        "adb_serial": "127.0.0.1:7555",
        "adb_connect": "127.0.0.1:7555",
        "adb_screenshot_method": "screencap",
    }
    cfg = AppConfig._from_dict(raw)
    assert cfg.adb_path == "C:/tools/adb.exe"
    assert cfg.adb_serial == "127.0.0.1:7555"
    assert cfg.adb_connect == "127.0.0.1:7555"


def test_validate_rejects_bad_platform():
    cfg = AppConfig._from_dict({"platform": "ios"})
    assert any("platform" in w for w in cfg.validate())


def test_validate_accepts_adb():
    cfg = AppConfig._from_dict({"platform": "adb"})
    assert not any("platform" in w for w in cfg.validate())
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `pytest tests/test_config.py -v`
Expected: FAIL(`AttributeError: 'AppConfig' object has no attribute 'platform'`)

- [ ] **Step 3: 實作**

修改 `config.py`:

在 `_VALID_INPUT_BACKENDS` 後新增常數:

```python
_VALID_PLATFORMS = ("windows", "adb")
_VALID_ADB_SCREENSHOT_METHODS = ("screencap",)
```

在 `AppConfig` 的「基礎設定」區塊(`input_backend` 之後)新增欄位:

```python
    # ---- 平台 ----
    platform: str = "windows"                  # windows / adb
    adb_path: str | None = None                # None = 用 PATH
    adb_serial: str | None = None              # None = 自動取唯一設備
    adb_connect: str | None = None             # 如 "127.0.0.1:7555";None=不 connect
    adb_screenshot_method: str = "screencap"   # 預留擴展點
```

在 `validate()` 內(`input_backend` 校驗之後)新增:

```python
        if self.platform not in _VALID_PLATFORMS:
            warnings.append(
                f"不支持的平台: {self.platform}，可选: {_VALID_PLATFORMS}"
            )
        if self.adb_screenshot_method not in _VALID_ADB_SCREENSHOT_METHODS:
            warnings.append(
                f"不支持的 ADB 截图方式: {self.adb_screenshot_method}，可选: {_VALID_ADB_SCREENSHOT_METHODS}"
            )
```

> `_coerce` 對 `str | None` 型別欄位不轉換(只處理 int/float),None 與字串原樣通過,正確。

- [ ] **Step 4: 跑測試確認通過**

Run: `pytest tests/test_config.py -v`
Expected: PASS(全部)

- [ ] **Step 5: 提交**

```bash
git add config.py tests/test_config.py
git commit -m "feat(config): 新增 platform 與 adb_* 欄位及校驗"
```

---

## Task 2: device/base.py — 純函數 + DeviceBackend 抽象

**Files:**
- Create: `device/__init__.py`(空套件佔位,本任務僅放 docstring)
- Create: `device/base.py`
- Test: `tests/test_device.py`

- [ ] **Step 1: 建立 device 套件**

`device/__init__.py` 內容(本任務先留空殼,Task 6 再填 factory):

```python
"""設備門面套件 — 統一截圖/輸入/縮放/生命週期,平台無關。"""
```

- [ ] **Step 2: 寫失敗測試**

建立 `tests/test_device.py`:

```python
"""device 層測試 — 純函數解析/縮放 + factory + 後端委派。"""
import numpy as np
import pytest

from device.base import (
    DeviceError,
    parse_wm_size,
    parse_devices,
    scale_ref_to_device,
    select_serial,
)


def test_parse_wm_size():
    assert parse_wm_size("Physical size: 1920x1080") == (1920, 1080)
    assert parse_wm_size("Physical size: 1280x720") == (1280, 720)


def test_parse_wm_size_invalid():
    with pytest.raises(DeviceError):
        parse_wm_size("no size info here")


def test_parse_devices_filters_unusable():
    text = (
        "List of devices attached\n"
        "127.0.0.1:7555\tdevice\n"
        "emulator-5554\toffline\n"
        "usb123\tunauthorized\n"
    )
    assert parse_devices(text) == ["127.0.0.1:7555"]


def test_scale_ref_to_device_1080p_identity():
    assert scale_ref_to_device(960, 540, 1920, 1080) == (960, 540)


def test_scale_ref_to_device_720p():
    # 960/1920*1280 = 640; 540/1080*720 = 360
    assert scale_ref_to_device(960, 540, 1280, 720) == (640, 360)


def test_select_serial_preferred():
    assert select_serial(["a", "b"], "b") == "b"


def test_select_serial_single():
    assert select_serial(["only"], None) == "only"


def test_select_serial_none_raises():
    with pytest.raises(DeviceError):
        select_serial([], None)


def test_select_serial_multiple_raises():
    with pytest.raises(DeviceError):
        select_serial(["a", "b"], None)
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `pytest tests/test_device.py -v`
Expected: FAIL(`ModuleNotFoundError: No module named 'device.base'`)

- [ ] **Step 4: 實作 base.py**

建立 `device/base.py`:

```python
"""DeviceBackend 抽象基類 + 純函數(解析/縮放),無 IO 副作用,易單測。"""

from __future__ import annotations

import re
from abc import ABC, abstractmethod

import numpy as np

from capture import REF_WIDTH, REF_HEIGHT


class DeviceError(Exception):
    """設備相關錯誤(連線/解析/找不到設備等)。"""


def parse_wm_size(text: str) -> tuple[int, int]:
    """解析 `adb shell wm size` 輸出,回傳 (width, height)。"""
    m = re.search(r"(\d+)\s*[xX]\s*(\d+)", text)
    if not m:
        raise DeviceError(f"無法解析 wm size: {text!r}")
    return int(m.group(1)), int(m.group(2))


def parse_devices(text: str) -> list[str]:
    """解析 `adb devices` 輸出,回傳狀態為 'device' 的可用 serial 列表。"""
    serials: list[str] = []
    for line in text.splitlines():
        line = line.strip()
        if not line or line.startswith("List of devices"):
            continue
        parts = line.split()
        if len(parts) >= 2 and parts[1] == "device":
            serials.append(parts[0])
    return serials


def scale_ref_to_device(
    ref_x: float, ref_y: float, dev_w: int, dev_h: int
) -> tuple[int, int]:
    """參考解析度座標(1920×1080)→ 設備實際座標。"""
    return round(ref_x * dev_w / REF_WIDTH), round(ref_y * dev_h / REF_HEIGHT)


def select_serial(available: list[str], preferred: str | None) -> str:
    """依偏好/可用清單決定 serial:指定優先 → 唯一自動取 → 0/多台報錯。"""
    if preferred is not None:
        return preferred
    if len(available) == 0:
        raise DeviceError("找不到任何 adb 設備")
    if len(available) > 1:
        raise DeviceError(
            f"偵測到多個設備 {available}，請在 config 指定 adb_serial"
        )
    return available[0]


class DeviceBackend(ABC):
    """統一設備門面。

    契約:
      - capture() 恆輸出 BGR ndarray,形狀 (REF_HEIGHT, REF_WIDTH, 3)。
      - click/swipe 吃「參考解析度座標」(基於 1920×1080),由實作內部縮放。
      - 上層 flow 與平台無關。
    """

    @abstractmethod
    def capture(self) -> np.ndarray: ...

    @abstractmethod
    def click(self, ref_x: float, ref_y: float) -> None: ...

    @abstractmethod
    def double_click(self, ref_x: float, ref_y: float) -> None: ...

    @abstractmethod
    def swipe(
        self, x1: float, y1: float, x2: float, y2: float, duration: float = 0.1
    ) -> None: ...

    def prepare(self) -> None:
        """前置(Windows=SetForegroundWindow;ADB=連線檢查)。預設 noop。"""

    def close(self) -> None:
        """釋放資源。預設 noop。"""

    @property
    def resolution(self) -> tuple[int, int]:
        """設備實際解析度(日誌/偵錯)。子類覆寫。"""
        return REF_WIDTH, REF_HEIGHT
```

- [ ] **Step 5: 跑測試確認通過**

Run: `pytest tests/test_device.py -v`
Expected: PASS(9 個純函數測試)

- [ ] **Step 6: 提交**

```bash
git add device/__init__.py device/base.py tests/test_device.py
git commit -m "feat(device): DeviceBackend 抽象 + 純函數(解析/縮放/選 serial)"
```

---

## Task 3: device/adb_client.py — AdbClient

**Files:**
- Create: `device/adb_client.py`
- Test: `tests/test_device.py`(擴充)

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_device.py` 頂部新增 import:

```python
from unittest.mock import MagicMock

from device.adb_client import AdbClient
```

在檔案末尾新增:

```python
def _client_with_devices(monkeypatch, devices_text, adb="adb"):
    """建 AdbClient 但攔截 subprocess,devices 回 devices_text。"""
    calls = []

    def fake_run(cmd, timeout=10, **kwargs):
        calls.append(cmd)
        if "devices" in cmd:
            return devices_text
        if "wm size" in "".join(cmd):
            return "Physical size: 1920x1080"
        return ""

    client_cls = AdbClient
    monkeypatch.setattr("device.adb_client.subprocess.run", fake_run)
    monkeypatch.setattr("device.adb_client.shutil.which", lambda x: adb)
    return client_cls, calls


def test_adb_client_picks_single_device(monkeypatch):
    cls, _ = _client_with_devices(monkeypatch, "List of devices attached\nserial1\tdevice\n")
    client = cls(serial=None)
    assert client._serial == "serial1"


def test_adb_client_uses_preferred_serial(monkeypatch):
    cls, _ = _client_with_devices(monkeypatch, "List of devices attached\na\tdevice\nb\tdevice\n")
    client = cls(serial="b")
    assert client._serial == "b"


def test_adb_client_no_device_raises(monkeypatch):
    cls, _ = _client_with_devices(monkeypatch, "List of devices attached\n")
    with pytest.raises(DeviceError):
        cls(serial=None)


def test_adb_client_missing_adb_raises(monkeypatch):
    monkeypatch.setattr("device.adb_client.shutil.which", lambda x: None)
    with pytest.raises(DeviceError):
        AdbClient(serial=None)


def test_adb_client_tap_builds_command(monkeypatch):
    cls, calls = _client_with_devices(monkeypatch, "List of devices attached\ns\tdevice\n")
    client = cls(serial="s")
    client.tap(640, 360)
    last = calls[-1]
    assert "input" in last and "tap" in last and "640" in last and "360" in last


def test_adb_client_wm_size(monkeypatch):
    cls, _ = _client_with_devices(
        monkeypatch, "List of devices attached\ns\tdevice\n"
    )
    client = cls(serial="s")
    assert client.wm_size() == (1920, 1080)


def test_adb_client_connect_called(monkeypatch):
    calls = []
    monkeypatch.setattr("device.adb_client.subprocess.run", lambda cmd, timeout=10, **kwargs: (calls.append(cmd), "ok")[1])
    monkeypatch.setattr("device.adb_client.shutil.which", lambda x: "adb")
    monkeypatch.setattr(
        "device.adb_client.parse_devices",
        lambda text: ["127.0.0.1:7555"],
    )
    client = AdbClient(connect="127.0.0.1:7555", serial="127.0.0.1:7555")
    assert any("connect" in c for c in calls)
    assert client._serial == "127.0.0.1:7555"
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `pytest tests/test_device.py -v`
Expected: FAIL(`ModuleNotFoundError: No module named 'device.adb_client'`)

- [ ] **Step 3: 實作 adb_client.py**

建立 `device/adb_client.py`:

```python
"""AdbClient — adb 子進程封裝。

遮蔽 subprocess 細節:路徑定位、connect、devices、exec-out、wm size、tap/swipe。
"""

from __future__ import annotations

import shutil
import subprocess

from device.base import (
    DeviceError,
    parse_devices,
    parse_wm_size,
    select_serial,
)


class AdbClient:
    """對單一 adb 設備的指令封裝。"""

    def __init__(
        self,
        adb_path: str | None = None,
        connect: str | None = None,
        serial: str | None = None,
    ):
        # 1. 定位 adb
        self._adb = adb_path or shutil.which("adb") or shutil.which("adb.exe")
        if not self._adb:
            raise DeviceError("找不到 adb，請安裝或在 config 指定 adb_path")

        # 2. 模擬器/WiFi 連線
        if connect:
            self._run_global(["connect", connect])

        # 3. 決定 serial
        if serial is None:
            serial = select_serial(self.devices(), None)
        self._serial = serial

    # ---- 底層執行 ----

    def _run_global(self, args: list[str], timeout: float = 10.0) -> str:
        """不帶 serial 的 adb 命令(devices/connect)。"""
        try:
            res = subprocess.run(
                [self._adb, *args], capture_output=True, text=True, timeout=timeout
            )
            if res.returncode != 0:
                raise DeviceError(f"adb 命令失敗 {' '.join(args)}: {res.stderr.strip()}")
            return res.stdout
        except FileNotFoundError as e:
            raise DeviceError(f"adb 執行失敗: {e}") from e
        except subprocess.TimeoutExpired as e:
            raise DeviceError(f"adb 命令逾時 {' '.join(args)}") from e

    def run(self, args: list[str], timeout: float = 10.0) -> str:
        """帶 -s serial 的 shell 命令,回傳 stdout 文字。"""
        return self._run_global(["-s", self._serial, *args], timeout=timeout)

    def exec_out(self, args: list[str], timeout: float = 10.0) -> bytes:
        """exec-out(二進制安全),回傳 bytes。"""
        try:
            res = subprocess.run(
                [self._adb, "-s", self._serial, "exec-out", *args],
                capture_output=True, timeout=timeout,
            )
            if res.returncode != 0:
                raise DeviceError(f"exec-out 失敗 {' '.join(args)}: {res.stderr.decode(errors='replace').strip()}")
            return res.stdout
        except subprocess.TimeoutExpired as e:
            raise DeviceError(f"exec-out 逾時 {' '.join(args)}") from e

    # ---- 高階能力 ----

    def devices(self) -> list[str]:
        return parse_devices(self._run_global(["devices"]))

    def wm_size(self) -> tuple[int, int]:
        return parse_wm_size(self.run(["shell", "wm", "size"]))

    def tap(self, x: int, y: int) -> None:
        self.run(["shell", "input", "tap", str(x), str(y)])

    def swipe(self, x1: int, y1: int, x2: int, y2: int, duration_ms: int) -> None:
        self.run(
            ["shell", "input", "swipe", str(x1), str(y1), str(x2), str(y2), str(duration_ms)]
        )
```

- [ ] **Step 4: 跑測試確認通過**

Run: `pytest tests/test_device.py -v`
Expected: PASS(含 7 個新 AdbClient 測試)

- [ ] **Step 5: 提交**

```bash
git add device/adb_client.py tests/test_device.py
git commit -m "feat(device): AdbClient adb 子進程封裝"
```

---

## Task 4: device/adb.py — AdbDeviceBackend

**Files:**
- Create: `device/adb.py`
- Test: `tests/test_device.py`(擴充)

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_device.py` 頂部 import 區新增:

```python
from device.adb import AdbDeviceBackend
```

末尾新增(用真實 PNG bytes 確保 imdecode 路徑被驗證):

```python
def _png_bytes_of(shape=(720, 1280, 3)):
    """產生一張固定位元 PNG bytes,供 capture 測試。"""
    import cv2
    img = (np.zeros(shape, dtype=np.uint8) + 7)  # 非零灰階
    ok, buf = cv2.imencode(".png", img)
    assert ok
    return buf.tobytes()


def _adb_device(monkeypatch, wm="Physical size: 1280x720"):
    client = MagicMock()
    client.wm_size.return_value = __import__("device.base", fromlist=["parse_wm_size"]).parse_wm_size(wm)
    return AdbDeviceBackend(client), client


def test_adb_device_capture_resizes_to_1080p(monkeypatch):
    dev, client = _adb_device(monkeypatch)
    client.exec_out.return_value = _png_bytes_of((720, 1280, 3))
    img = dev.capture()
    assert img.shape == (1080, 1920, 3)


def test_adb_device_click_scales(monkeypatch):
    dev, client = _adb_device(monkeypatch, "Physical size: 1280x720")
    dev.click(960, 540)
    client.tap.assert_called_once_with(640, 360)


def test_adb_device_swipe_converts_duration_to_ms(monkeypatch):
    dev, client = _adb_device(monkeypatch, "Physical size: 1920x1080")
    dev.swipe(100, 200, 100, 800, duration=0.3)
    client.swipe.assert_called_once_with(100, 200, 100, 800, 300)


def test_adb_device_double_click_two_taps(monkeypatch):
    dev, client = _adb_device(monkeypatch, "Physical size: 1920x1080")
    dev.double_click(960, 540)
    assert client.tap.call_count == 2
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `pytest tests/test_device.py -v`
Expected: FAIL(`ModuleNotFoundError: No module named 'device.adb'`)

- [ ] **Step 3: 實作 adb.py**

建立 `device/adb.py`:

```python
"""AdbDeviceBackend — 以 adb 指令截圖/輸入的設備後端。"""

from __future__ import annotations

import time

import cv2
import numpy as np

from capture import REF_WIDTH, REF_HEIGHT
from device.adb_client import AdbClient
from device.base import DeviceBackend, scale_ref_to_device


class AdbDeviceBackend(DeviceBackend):
    def __init__(self, client: AdbClient):
        self._client = client
        self._w, self._h = client.wm_size()        # 啟動偵測一次,快取
        self._sx = self._w / REF_WIDTH
        self._sy = self._h / REF_HEIGHT

    def capture(self) -> np.ndarray:
        # exec-out: 二進制透傳,無 \r\n 污染(shell 模式才會壞 PNG)
        png = self._client.exec_out(["screencap", "-p"])
        img = cv2.imdecode(np.frombuffer(png, np.uint8), cv2.IMREAD_COLOR)
        if img is None:
            from device.base import DeviceError
            raise DeviceError("adb screencap 解碼失敗")
        if (self._w, self._h) != (REF_WIDTH, REF_HEIGHT):
            img = cv2.resize(img, (REF_WIDTH, REF_HEIGHT))
        return img

    def click(self, ref_x: float, ref_y: float) -> None:
        dx, dy = scale_ref_to_device(ref_x, ref_y, self._w, self._h)
        self._client.tap(dx, dy)

    def double_click(self, ref_x: float, ref_y: float) -> None:
        # adb 無原生雙擊 → 兩次 tap,保持與 Windows 行為等價
        self.click(ref_x, ref_y)
        time.sleep(0.05)
        self.click(ref_x, ref_y)

    def swipe(
        self, x1: float, y1: float, x2: float, y2: float, duration: float = 0.1
    ) -> None:
        sx1, sy1 = scale_ref_to_device(x1, y1, self._w, self._h)
        sx2, sy2 = scale_ref_to_device(x2, y2, self._w, self._h)
        self._client.swipe(sx1, sy1, sx2, sy2, int(duration * 1000))

    @property
    def resolution(self) -> tuple[int, int]:
        return self._w, self._h
```

- [ ] **Step 4: 跑測試確認通過**

Run: `pytest tests/test_device.py -v`
Expected: PASS(含 4 個新 AdbDeviceBackend 測試)

- [ ] **Step 5: 提交**

```bash
git add device/adb.py tests/test_device.py
git commit -m "feat(device): AdbDeviceBackend 截圖/點擊/滑動 + 縮放"
```

---

## Task 5: device/windows.py — WindowsDeviceBackend + 遷移 find_game_window

**Files:**
- Create: `device/windows.py`
- Modify: `worker.py`(改 import find_game_window 來源)
- Test: `tests/test_device.py`(擴充,結構性委派測試)

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_device.py` import 區新增:

```python
from device.windows import WindowsDeviceBackend
```

末尾新增(mock capture_window/scale_to_client/SendInputBackend,驗證委派鏈):

```python
def test_windows_device_click_delegates(monkeypatch):
    captured = {}

    monkeypatch.setattr("device.windows.scale_to_client", lambda hwnd, x, y: (x * 2, y * 2))
    fake_input = MagicMock()
    monkeypatch.setattr("device.windows.SendInputBackend", lambda: fake_input)

    dev = WindowsDeviceBackend(hwnd=42, capture_method="bitblt")
    dev.double_click(100, 50)

    assert fake_input.double_click.call_count == 1
    args = fake_input.double_click.call_args[0]
    assert args[0] == 42          # hwnd 透傳
    assert args[1] == 200 and args[2] == 100   # scale_to_client 後座標


def test_windows_device_capture_delegates(monkeypatch):
    expected = np.zeros((1080, 1920, 3), dtype=np.uint8)
    monkeypatch.setattr("device.windows.capture_window", lambda hwnd, m: expected)
    monkeypatch.setattr("device.windows.SendInputBackend", lambda: MagicMock())
    dev = WindowsDeviceBackend(hwnd=1, capture_method="bitblt")
    assert dev.capture() is expected


def test_windows_device_close_calls_close_all(monkeypatch):
    called = {"n": 0}
    monkeypatch.setattr("device.windows.close_all", lambda: called.__setitem__("n", called["n"] + 1))
    monkeypatch.setattr("device.windows.SendInputBackend", lambda: MagicMock())
    WindowsDeviceBackend(hwnd=1).close()
    assert called["n"] == 1
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `pytest tests/test_device.py -v`
Expected: FAIL(`ModuleNotFoundError: No module named 'device.windows'`)

- [ ] **Step 3: 實作 windows.py(含自 worker 遷入的 find_game_window)**

建立 `device/windows.py`:

```python
"""WindowsDeviceBackend — 組合 BitBlt/MSS 截圖 + SendInput 輸入 + 客戶區縮放。

行為與舊版直接呼叫 capture_window/sendinput 完全一致,僅收攏到 DeviceBackend 介面。
"""

from __future__ import annotations

import win32gui

from capture import REF_WIDTH, REF_HEIGHT
from capture import capture_window
from capture.bitblt import close_all
from input.base import scale_to_client
from input.sendinput import SendInputBackend
from device.base import DeviceBackend
import numpy as np


def find_game_window(window_title: str) -> int | None:
    """查找遊戲視窗句柄(自 worker.py 遷入)。

    回傳面積最大且標題包含關鍵字的可見視窗 HWND,未找到回 None。
    """
    candidates: list[tuple[int, int]] = []

    def callback(hwnd, _):
        if win32gui.IsWindowVisible(hwnd) and window_title in win32gui.GetWindowText(hwnd):
            rect = win32gui.GetClientRect(hwnd)
            candidates.append((rect[2] * rect[3], hwnd))

    win32gui.EnumWindows(callback, None)
    if not candidates:
        return None
    candidates.sort(reverse=True)
    return candidates[0][1]


class WindowsDeviceBackend(DeviceBackend):
    def __init__(self, hwnd: int, capture_method: str = "auto"):
        self._hwnd = hwnd
        self._capture_method = capture_method
        self._input = SendInputBackend()

    def capture(self) -> np.ndarray:
        return capture_window(self._hwnd, self._capture_method)

    def click(self, ref_x: float, ref_y: float) -> None:
        x, y = scale_to_client(self._hwnd, ref_x, ref_y)
        self._input.click(self._hwnd, x, y)

    def double_click(self, ref_x: float, ref_y: float) -> None:
        x, y = scale_to_client(self._hwnd, ref_x, ref_y)
        self._input.double_click(self._hwnd, x, y)

    def swipe(
        self, x1: float, y1: float, x2: float, y2: float, duration: float = 0.1
    ) -> None:
        sx1, sy1 = scale_to_client(self._hwnd, x1, y1)
        sx2, sy2 = scale_to_client(self._hwnd, x2, y2)
        self._input.swipe(self._hwnd, sx1, sy1, sx2, sy2, duration)

    def prepare(self) -> None:
        try:
            win32gui.SetForegroundWindow(self._hwnd)
        except Exception:
            pass  # 視窗可能已在前台

    def close(self) -> None:
        close_all()

    @property
    def resolution(self) -> tuple[int, int]:
        rect = win32gui.GetClientRect(self._hwnd)
        return rect[2], rect[3]
```

- [ ] **Step 4: worker.py 改 import 來源(避免重複定義)**

修改 `worker.py`:

刪除整個 `find_game_window` 函式定義(原第 25-46 行),改在檔案頂部 import 區把

```python
from automation.flow import ShopFlow
```

之前新增一行(並移除不再需要的 `import win32gui` 與 `from capture.bitblt import close_all`,這兩者本任務先保留也可,Task 9 會清掉;此處先只動 find_game_window 來源):

實際上 `worker.run()` 目前尚未改用 device,仍直接呼叫 `find_game_window` 與 `close_all`。為保持本任務綠,**先在 worker.py 頂部加入**:

```python
from device.windows import find_game_window
```

並**刪除 worker.py 內原本的 `find_game_window` 函式定義**(第 25-46 行)與頂部 `import win32gui`(若僅 find_game_window 使用)。`close_all` import 與 `win32gui` 若尚有他處使用則保留;否則一併移除 `import win32gui`。

> 驗證:`grep -n "win32gui\|find_game_window\|close_all" worker.py` 確認 worker 內已無重複定義,且 import 來自 device.windows / capture.bitblt。

- [ ] **Step 5: 跑全部測試確認通過**

Run: `pytest -v`
Expected: PASS(含 3 個新 WindowsDeviceBackend 測試;既有測試不受影響 —— worker 行為不變)

- [ ] **Step 6: 提交**

```bash
git add device/windows.py worker.py tests/test_device.py
git commit -m "feat(device): WindowsDeviceBackend + find_game_window 遷入 device"
```

---

## Task 6: device/__init__.py — create_device factory

**Files:**
- Modify: `device/__init__.py`
- Test: `tests/test_device.py`(擴充)

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_device.py` import 區新增:

```python
from device import create_device
from unittest.mock import MagicMock
```

末尾新增:

```python
def test_create_device_windows(monkeypatch):
    monkeypatch.setattr("device.WindowsDeviceBackend", lambda hwnd, capture_method="auto": ("win", hwnd, capture_method))
    monkeypatch.setattr("device.find_game_window", lambda title: 999)
    cfg = MagicMock(); cfg.platform = "windows"; cfg.window_title = "X"; cfg.capture_method = "bitblt"
    dev = create_device(cfg)
    assert dev == ("win", 999, "bitblt")


def test_create_device_windows_no_window_raises(monkeypatch):
    monkeypatch.setattr("device.find_game_window", lambda title: None)
    cfg = MagicMock(); cfg.platform = "windows"; cfg.window_title = "X"
    with pytest.raises(DeviceError):
        create_device(cfg)


def test_create_device_adb(monkeypatch):
    built = {}
    monkeypatch.setattr("device.AdbDeviceBackend", lambda client: built.setdefault("client", client))
    fake_client = object()
    monkeypatch.setattr("device.AdbClient", lambda **kw: fake_client)
    cfg = MagicMock()
    cfg.platform = "adb"; cfg.adb_path = None; cfg.adb_connect = None; cfg.adb_serial = None
    dev = create_device(cfg)
    assert built["client"] is fake_client


def test_create_device_unknown_platform_raises():
    cfg = MagicMock(); cfg.platform = "ios"
    with pytest.raises(DeviceError):
        create_device(cfg)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `pytest tests/test_device.py -v`
Expected: FAIL(`ImportError: cannot import name 'create_device' from 'device'`)

- [ ] **Step 3: 實作 __init__.py**

完整覆寫 `device/__init__.py`:

```python
"""設備門面套件 — 統一截圖/輸入/縮放/生命週期,平台無關。"""

from __future__ import annotations

from config import AppConfig
from device.base import DeviceBackend, DeviceError
from device.windows import WindowsDeviceBackend, find_game_window
from device.adb_client import AdbClient
from device.adb import AdbDeviceBackend

__all__ = ["DeviceBackend", "DeviceError", "create_device"]


def create_device(config: AppConfig) -> DeviceBackend:
    """依 config.platform 建立設備後端。"""
    if config.platform == "windows":
        hwnd = find_game_window(config.window_title)
        if not hwnd:
            raise DeviceError("找不到遊戲視窗，請確認遊戲已開啟")
        return WindowsDeviceBackend(hwnd, config.capture_method)
    if config.platform == "adb":
        client = AdbClient(
            adb_path=config.adb_path,
            connect=config.adb_connect,
            serial=config.adb_serial,
        )
        return AdbDeviceBackend(client)
    raise DeviceError(f"不支持的平台: {config.platform}")
```

- [ ] **Step 4: 跑測試確認通過**

Run: `pytest tests/test_device.py -v`
Expected: PASS(含 4 個 factory 測試)

- [ ] **Step 5: 提交**

```bash
git add device/__init__.py tests/test_device.py
git commit -m "feat(device): create_device 工廠依 platform 分派"
```

---

## Task 7: tests/fakes.py — FakeDevice

**Files:**
- Create: `tests/fakes.py`

(純測試基礎設施,供 Task 8 的 flow 測試使用。)

- [ ] **Step 1: 實作**

建立 `tests/fakes.py`:

```python
"""測試替身 — FakeDevice 用於 flow 整合測試,無 IO。"""
from __future__ import annotations

import numpy as np

from device.base import DeviceBackend


class FakeDevice(DeviceBackend):
    """可程式化的假設備:capture 回固定圖,點擊/滑動記錄於 calls。"""

    def __init__(self, image: np.ndarray | None = None):
        self._image = image if image is not None else np.zeros(
            (1080, 1920, 3), dtype=np.uint8
        )
        self.calls: list[tuple] = []

    def set_image(self, image: np.ndarray) -> None:
        self._image = image

    def capture(self) -> np.ndarray:
        return self._image

    def click(self, ref_x: float, ref_y: float) -> None:
        self.calls.append(("click", ref_x, ref_y))

    def double_click(self, ref_x: float, ref_y: float) -> None:
        self.calls.append(("double_click", ref_x, ref_y))

    def swipe(self, x1, y1, x2, y2, duration=0.1) -> None:
        self.calls.append(("swipe", x1, y1, x2, y2, duration))
```

- [ ] **Step 2: 確認可 import**

Run: `python -c "from tests.fakes import FakeDevice; FakeDevice()"`
Expected: 無輸出(成功)

- [ ] **Step 3: 提交**

```bash
git add tests/fakes.py
git commit -m "test: FakeDevice 測試替身"
```

---

## Task 8: state.py + flow.py — hwnd→device 重構(核心)

> 本任務改動互相耦合(state 定義 `device`、flow 使用 `ctx.device`/注入 `device`),須一起改才能保持測試綠。先改測試(紅),再改實作(綠)。

**Files:**
- Modify: `automation/state.py`
- Modify: `automation/flow.py`
- Modify: `tests/test_state.py`
- Modify: `tests/test_flow.py`

- [ ] **Step 1: 改 test_state.py(寫新期望)**

`tests/test_state.py` 完整內容:

```python
from unittest.mock import MagicMock
from automation.state import ShopContext


def _ctx(mode=1, money=10**8, stone=100, expect_num=5, **kw):
    return ShopContext(device=MagicMock(), mode=mode, expect_num=expect_num,
                       money=money, stone=stone, **kw)


def test_should_continue_ok():
    assert _ctx().should_continue is True


def test_should_continue_mode1_money():
    assert _ctx(mode=1, money=184000).should_continue is False
    assert _ctx(mode=1, money=184001).should_continue is True


def test_should_continue_mode2_money():
    assert _ctx(mode=2, money=280000).should_continue is False
    assert _ctx(mode=2, money=280001).should_continue is True


def test_should_continue_mode3_budget():
    assert _ctx(mode=3, expect_num=3).should_continue is True
    assert _ctx(mode=3, expect_num=2).should_continue is False


def test_should_continue_stone_for_refresh():
    assert _ctx(mode=1, stone=2).should_continue is False


def test_total_money_used():
    c = _ctx()
    c.covenant_bought = 2
    c.mystic_bought = 1
    assert c.total_money_used == 2 * 184000 + 1 * 280000


def test_total_stone_used():
    c = _ctx()
    c.refresh_count = 4
    assert c.total_stone_used == 12


def test_ctx_holds_device():
    dev = MagicMock()
    c = ShopContext(device=dev, mode=1, expect_num=1, money=10**8, stone=100)
    assert c.device is dev
```

(原 `test_capture_method_default` 移除 —— capture_method 已從 ctx 移出。)

- [ ] **Step 2: 改 test_flow.py(注入 FakeDevice)**

`tests/test_flow.py` 完整內容:

```python
"""flow 整合測試 — 注入 FakeDevice/mock matcher,驗證狀態流轉與計費。"""
import numpy as np
import pytest
from unittest.mock import MagicMock

from automation.state import ShopContext, ShopState
from automation.flow import ShopFlow
from detection.matcher import MatchResult
from tests.fakes import FakeDevice


def _match_result(x=500, y=400):
    return MatchResult(name="t", score=0.95, box=(x, y, 10, 10))


def _mk_flow(mode=1, expect_num=2):
    device = FakeDevice()
    ctx = ShopContext(device=device, mode=mode, expect_num=expect_num,
                      money=10**8, stone=100)
    config = MagicMock()
    config.match_threshold_location = 0.9
    config.match_threshold_button = 0.85
    config.match_threshold_confirm = 0.9
    config.match_threshold_refresh = 0.8
    config.wait_timeout = 0.1
    config.wait_timeout_long = 0.2
    config.max_retry = 2
    config.swipe_fail_limit = 5
    config.short_sleep_base = 0.0
    config.scan_roi_tuple = None
    config.button_roi_tuple = None

    templates = MagicMock()
    matcher = MagicMock()
    logger = MagicMock()
    flow = ShopFlow(ctx, templates, matcher, device, logger, config)
    return flow, ctx, matcher, device


def test_scanning_finds_covenant():
    flow, ctx, matcher, _ = _mk_flow(mode=1, expect_num=2)
    ctx.state = ShopState.SCANNING
    matcher.match.return_value = _match_result(300, 200)
    flow._handle_scanning()

    assert ctx.state == ShopState.BUYING_COVENANT
    assert ctx.target is not None
    assert ctx.target.label == "聖約"


def test_buying_covenant_success():
    flow, ctx, matcher, device = _mk_flow(mode=1, expect_num=2)
    ctx.state = ShopState.BUYING_COVENANT
    ctx.target = type("T", (), {"match_center": (100, 100), "label": "聖約"})

    matcher.match.side_effect = [_match_result(900, 140), None]
    flow._handle_buying()

    assert ctx.covenant_bought == 1
    assert ctx.covenant_found is True
    assert ctx.expect_num == 1
    assert ctx.money == 10**8 - 184000
    assert ctx.state == ShopState.SCANNING
    # 點擊經 device,座標為參考座標(含偏移)
    assert any(c[0] == "double_click" for c in device.calls)


def test_buying_confirm_timeout_returns_to_scan():
    flow, ctx, matcher, _ = _mk_flow(mode=1, expect_num=2)
    ctx.state = ShopState.BUYING_COVENANT
    ctx.target = type("T", (), {"match_center": (100, 100), "label": "聖約"})

    matcher.match.side_effect = [_match_result()] * 100
    flow._handle_buying()

    assert ctx.covenant_bought == 0
    assert ctx.state == ShopState.SCANNING
```

- [ ] **Step 3: 跑測試確認失敗**

Run: `pytest tests/test_state.py tests/test_flow.py -v`
Expected: FAIL(state: `unexpected keyword argument 'device'` / flow: `unexpected keyword argument 'device'`)

- [ ] **Step 4: 改 state.py**

`automation/state.py` 的 `ShopContext` dataclass:

把欄位區塊

```python
    # ---- 窗口信息 ----
    hwnd: int
```

改為

```python
    # ---- 設備 ----
    device: "object"   # DeviceBackend 實例(避免循環 import,僅標註)
```

並移除欄位

```python
    capture_method: str = "auto"  # 截图方式（auto/bitblt/mss）
```

(整行刪除。)`should_continue`/`stop`/`total_*` 等不涉及 hwnd/capture_method,不動。

- [ ] **Step 5: 改 flow.py**

5a. 頂部 import 採行級調整(其餘 import 一律保留不動):

- **刪除**這一行:

```python
import win32gui
```

- **刪除**這一行:

```python
from capture import capture_window
```

- **把**

```python
from input.base import InputBackend
```

**改為**

```python
from device.base import DeviceBackend
```

> 保留不動的 import:`import cv2`、`import numpy as np`、`from config import AppConfig`、`from detection.matcher import TemplateMatcher`、`from automation.state import ShopContext, ShopState, BookmarkTarget`、`from automation.templates import TemplateManager`、`from logger import ShopLogger`、以及 `from constants import (...)`(COVENANT_COST / MYSTIC_COST / REFRESH_STONE_COST / BUY_CLICK_OFFSET_X/Y / SWIPE_START_REF / SWIPE_END_REF / SWIPE_DURATION / SWIPE_CHANGED_DIFF / min_money_for_mode)。**切勿誤刪 constants/templates/logger import,否則 flow 會 NameError。**

5b. `wait_for` / `wait_for_gone` / `wait_for_stable` 中所有

```python
        img = capture_window(ctx.hwnd, ctx.capture_method)
```

與

```python
    prev = capture_window(ctx.hwnd, ctx.capture_method)
```

改為

```python
        img = ctx.device.capture()
```

與

```python
    prev = ctx.device.capture()
```

(`wait_for_stable` 內有 prev 與 curr 兩處,都要改。)

5c. `ShopFlow.__init__` 簽名與賦值:

把

```python
    def __init__(
        self,
        ctx: ShopContext,
        templates: TemplateManager,
        matcher: TemplateMatcher,
        input_backend: InputBackend,
        logger: ShopLogger,
        config: AppConfig,
    ):
        self.ctx = ctx
        self.templates = templates
        self.matcher = matcher
        self.input = input_backend
        self.log = logger
        self.config = config
```

改為

```python
    def __init__(
        self,
        ctx: ShopContext,
        templates: TemplateManager,
        matcher: TemplateMatcher,
        device: DeviceBackend,
        logger: ShopLogger,
        config: AppConfig,
    ):
        self.ctx = ctx
        self.templates = templates
        self.matcher = matcher
        self.device = device
        self.log = logger
        self.config = config
```

5d. `_click_until_found` 中:

```python
        cx, cy = self.input.scale_coords(self.ctx.hwnd, *ref_pos)
        for i in range(max_retry):
            if not self.ctx._running:
                return None
            self.short_sleep(0.5)
            self.log.debug(f"點擊{name} ({cx:.0f},{cy:.0f}) 重試:{i+1}")
            self.input.double_click(self.ctx.hwnd, cx, cy)
```

改為

```python
        for i in range(max_retry):
            if not self.ctx._running:
                return None
            self.short_sleep(0.5)
            self.log.debug(f"點擊{name} {ref_pos} 重試:{i+1}")
            self.device.double_click(*ref_pos)
```

5e. `_click_until_gone` 中:

```python
        cx, cy = self.input.scale_coords(self.ctx.hwnd, *ref_pos)
        for i in range(self.config.max_retry):
            if not self.ctx._running:
                return True
            self.short_sleep(0.3)
            self.log.debug(f"點擊{name} ({cx:.0f},{cy:.0f}) 重試:{i+1}")
            self.input.double_click(self.ctx.hwnd, cx, cy)
```

改為

```python
        for i in range(self.config.max_retry):
            if not self.ctx._running:
                return True
            self.short_sleep(0.3)
            self.log.debug(f"點擊{name} {ref_pos} 重試:{i+1}")
            self.device.double_click(*ref_pos)
```

5f. `_handle_scanning` / `_handle_swiping` / `_handle_refreshing` 中所有

```python
        screenshot = capture_window(self.ctx.hwnd, self.ctx.capture_method)
        ...
        before = capture_window(self.ctx.hwnd, self.ctx.capture_method)
        ...
        after = capture_window(self.ctx.hwnd, self.ctx.capture_method)
        ...
        before_refresh = capture_window(self.ctx.hwnd, self.ctx.capture_method)
```

分別改為 `self.ctx.device.capture()`(scanning 的 `screenshot`、swiping 的 `before`/`after`、refreshing 的 `screenshot` 與 `before_refresh`,共 4 處)。

5g. `_handle_swiping` 中:

```python
        sx1, sy1 = self.input.scale_coords(self.ctx.hwnd, *SWIPE_START_REF)
        sx2, sy2 = self.input.scale_coords(self.ctx.hwnd, *SWIPE_END_REF)
        self.input.swipe(self.ctx.hwnd, sx1, sy1, sx2, sy2, SWIPE_DURATION)
```

改為

```python
        self.device.swipe(*SWIPE_START_REF, *SWIPE_END_REF, SWIPE_DURATION)
```

5h. `_handle_refreshing` 中:

```python
            srx, sry = self.input.scale_coords(self.ctx.hwnd, rx, ry)
            self.log.debug(f"點擊刷新按鈕 ({srx:.0f},{sry:.0f}) 重試:{retry+1}")
            self.input.double_click(self.ctx.hwnd, srx, sry)
```

改為

```python
            self.log.debug(f"點擊刷新按鈕 {(rx, ry)} 重試:{retry+1}")
            self.device.double_click(rx, ry)
```

5i. `_init()` 中:

```python
        try:
            win32gui.SetForegroundWindow(self.ctx.hwnd)
        except Exception:
            pass  # 視窗可能已在前台
```

改為

```python
        self.device.prepare()
```

5j. 確認無殘留:`grep -n "self.input\|self.ctx.hwnd\|capture_window\|win32gui\|scale_coords" automation/flow.py`
Expected: 無輸出(全部已移除)。

- [ ] **Step 6: 跑全部測試確認通過**

Run: `pytest -v`
Expected: PASS(state、flow、device、config、其他全綠)

- [ ] **Step 7: 提交**

```bash
git add automation/state.py automation/flow.py tests/test_state.py tests/test_flow.py
git commit -m "refactor: ShopContext/flow 改用 DeviceBackend,移除 hwnd 依賴"
```

---

## Task 9: worker.py — create_device 組裝

**Files:**
- Modify: `worker.py`

(IO 層;靠既有結構 + 手動驗證。)

- [ ] **Step 1: 改 worker.run()**

`worker.py` 的 import 區,把

```python
import win32gui

from PyQt6 import QtCore

from automation.flow import ShopFlow
from automation.state import ShopContext, ShopState
from automation.templates import TemplateManager
from capture import REF_WIDTH, REF_HEIGHT
from capture.bitblt import close_all
from config import AppConfig
from detection.matcher import TemplateMatcher
from input import create_backend
from logger import ShopLogger
```

改為

```python
from PyQt6 import QtCore

from automation.flow import ShopFlow
from automation.state import ShopContext, ShopState
from automation.templates import TemplateManager
from config import AppConfig
from detection.matcher import TemplateMatcher
from device import create_device, DeviceError
from logger import ShopLogger
```

(移除 `win32gui`、`REF_WIDTH/REF_HEIGHT`、`close_all`、`create_backend`、`find_game_window` import —— Task 5 已把 find_game_window 遷走;若 Task 5 仍殘留這些 import,本步一併清掉。)

`run()` 方法主體改為:

```python
    def run(self) -> None:
        self.isStart.emit()

        try:
            # 加载配置
            config = AppConfig.load()

            # 创建日志器
            logger = ShopLogger(self.emitLog)

            # 建立設備(Windows:查找視窗;ADB:連線/選設備)
            try:
                device = create_device(config)
            except DeviceError as e:
                logger.error(f"錯誤: {e}")
                raise

            # 创建上下文
            ctx = ShopContext(
                device=device,
                mode=self.startMode,
                expect_num=self.expectNum,
                money=self.moneyNum,
                stone=self.stoneNum,
            )
            self._ctx = ctx
            self._running = True

            # 初始化组件
            templates = TemplateManager(config.language)
            matcher = TemplateMatcher()

            # 创建并执行流程
            flow = ShopFlow(ctx, templates, matcher, device, logger, config)

            result = flow.run()

            # 更新 UI 显示的最终数值
            self.emitMoney.emit(str(result.money))
            self.emitStone.emit(str(result.stone))

            self.isFinish.emit()

        except Exception as e:
            self.emitLog.emit(f"錯誤: {e}")
            self.isError.emit()
        finally:
            if self._ctx is not None and self._ctx.device is not None:
                self._ctx.device.close()
            self._ctx = None
            self._running = False
```

> 變更要點:`find_game_window`/`hwnd`/`capture_method`/`create_backend` 移除,改 `create_device(config)`;`ctx` 用 `device=device`;`flow` 注入 `device`;`finally` 用 `device.close()` 替代 `close_all()`。

- [ ] **Step 2: 確認無殘留舊引用**

Run: `grep -n "find_game_window\|close_all\|create_backend\|ctx.hwnd\|hwnd=" worker.py`
Expected: 無輸出。

- [ ] **Step 3: 跑全部測試確認通過**

Run: `pytest -v`
Expected: PASS(worker 無單測,但其他全綠;import 鏈不壞即代表組裝正確)

- [ ] **Step 4: 提交**

```bash
git add worker.py
git commit -m "refactor(worker): 改用 create_device 組裝 + device.close 收尾"
```

---

## Task 10: main.py — 依 platform 決定提權

**Files:**
- Modify: `main.py`

(IO/bootstrap 層;靠手動驗證。)

- [ ] **Step 1: 改 main.py**

`main.py` 完整內容:

```python
"""第七史詩刷商店小工具 — 入口。

僅 Windows 原生模式(SendInput)需要管理員權限;ADB 模式為使用者級,跳過提權。
提權在導入 win32 之前執行,故以純 json 讀 config.json 判斷 platform(不依賴 pywin32)。
"""

import ctypes
import json
import sys
from pathlib import Path


def _needs_admin() -> bool:
    """僅 Windows 原生模式需管理員;讀取失敗時保守提權。"""
    try:
        if Path("config.json").exists():
            raw = json.loads(Path("config.json").read_text(encoding="utf-8"))
            return raw.get("platform", "windows") == "windows"
    except Exception:
        pass
    return True


if _needs_admin() and not ctypes.windll.shell32.IsUserAnAdmin():
    params = ' '.join([f'"{a}"' if ' ' in a else a for a in sys.argv])
    exe = sys.executable.replace("python.exe", "pythonw.exe")
    ctypes.windll.shell32.ShellExecuteW(None, "runas", exe, params, None, 1)
    sys.exit(0)

from PyQt6 import QtGui, QtWidgets

from gui import Ui_Main

if __name__ == "__main__":
    app = QtWidgets.QApplication(sys.argv)
    app.setWindowIcon(QtGui.QIcon("main.ico"))

    Main = QtWidgets.QWidget()
    ui = Ui_Main()
    ui.setupUi(Main)
    Main.show()
    sys.exit(app.exec())
```

- [ ] **Step 2: 語法檢查**

Run: `python -c "import ast; ast.parse(open('main.py', encoding='utf-8').read())"`
Expected: 無輸出(語法正確)。

- [ ] **Step 3: 提交**

```bash
git add main.py
git commit -m "feat(main): 依 platform 決定是否提權(ADB 模式免管理員)"
```

---

## Task 11: 端到端手動驗證

**Files:** 無(手動測試清單)

- [ ] **Step 1: Windows 模式回歸**

`config.json` 設(或維持預設)`"platform": "windows"`。

Run: `python main.py`
驗證:
- 正常提權(UAC 彈窗)
- GUI 啟動,點「開始」能找到遊戲視窗並執行刷商店流程(至少完成一次掃描→滑動)
- 行為與重構前一致

- [ ] **Step 2: ADB 模式 — 模擬器**

`config.json` 設:

```json
{
  "platform": "adb",
  "adb_connect": "127.0.0.1:7555",
  "adb_serial": "127.0.0.1:7555",
  "language": "zh-TW"
}
```

(模擬器實際 port 依各家:MuMu 7555 / 雷電 5555 / 夜神 62001 等,自行調整。)

前置:`adb connect 127.0.0.1:7555` 確認 `adb devices` 見該設備為 `device`。

Run: `python main.py`
驗證:
- 不彈 UAC(ADB 模式免提權)
- GUI 啟動,點「開始」後日誌顯示連上設備、開始刷商店
- 截圖/點擊/滑動在模擬器上生效(觀察遊戲實際被操作)
- 座標準確(模板能匹配、點擊命中按鈕)

- [ ] **Step 3: ADB 模式 — 錯誤路徑**

- 拔掉/關閉模擬器後點「開始」→ 日誌應顯示「找不到任何 adb 設備」之類中文錯誤,UI 回到可重試狀態。
- `adb_path` 指向不存在路徑 → 顯示「找不到 adb」錯誤。

- [ ] **Step 4: 更新文件**

更新 `CLAUDE.md` 的 Configuration 段,補上 `platform` / `adb_*` 欄位說明;Architecture 段補 `device/` 套件與「capture 恆輸出 1080×1920、點擊吃參考座標」契約。

```bash
git add CLAUDE.md
git commit -m "docs: 補充 ADB 模式與 device 套件說明"
```

---

## Self-Review(撰寫者自檢記錄)

**Spec 覆蓋:**
- DeviceBackend 抽象 → Task 2 ✓
- 純函數(parse/scale/select) → Task 2 ✓
- AdbClient → Task 3 ✓
- AdbDeviceBackend → Task 4 ✓
- WindowsDeviceBackend + find_game_window 遷移 → Task 5 ✓
- create_device factory → Task 6 ✓
- config platform/adb_* → Task 1 ✓
- ShopContext hwnd→device → Task 8 ✓
- flow 改 device(含 wait_for 系列、點擊、prepare、swipe) → Task 8 ✓
- worker 組裝 → Task 9 ✓
- main.py 提權 → Task 10 ✓
- 測試策略(FakeDevice、device 純邏輯、factory) → Task 2/3/4/5/6/7/8 ✓
- 範圍外(letterbox/重連/高效後端/GUI) → 設計文檔已記,本計劃不含 ✓

**型別一致性:** `DeviceBackend` 方法簽名、`create_device(config)`、`ShopContext(device=...)`、`ShopFlow(..., device, ...)`、`scale_ref_to_device(ref_x,ref_y,dev_w,dev_h)`、`AdbClient(adb_path,connect,serial)` 跨任務一致 ✓

**已知風險點(執行時留意):**
- Task 8 改動面最大,改完務必 `grep` 確認 flow 無 `self.input`/`hwnd`/`capture_window`/`win32gui`/`scale_coords` 殘留。
- `device/base.py` `from capture import REF_WIDTH, REF_HEIGHT` 依賴 pywin32 已安裝(本工具恆為 Windows 環境,成立)。
- ADB `exec-out screencap -p` 回傳 PNG 二進制;若特定 adb 版本行為差異,Task 11 手動驗證會暴露。
